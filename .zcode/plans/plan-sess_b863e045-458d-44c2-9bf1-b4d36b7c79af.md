# Implementation Plan: Audit Fixes (Critical + High, findings #1–25)

## Scope & Decisions (confirmed)
- **Scope:** Audit findings #1–25 (all 11 Critical + all 14 High correctness bugs). Medium/Low (#26–50) untouched.
- **#8 `.gam_bias_correct`:** Remove entirely (delete function + call site + its ~10 tests).
- **#11 bootstrap mode:** Add a new `resample_by = c("replicate","read")` API in R + a C++ replicate-resampling path; keep `"read"` (multinomial) as default for backward compatibility.

## Build / workflow notes
- `src/RcppExports.R` and `src/RcppExports.cpp` are auto-generated by `Rcpp::compileAttributes()` — I will re-run it after editing `resampling_rcpp.cpp` so the generated glue matches new/changed signatures.
- No test snapshots exist (`tests/testthat/_snaps/` is empty), so updating expectations is safe.
- `nlme`, `glmnet`, `MASS` already in Suggests.

---

## A. C++ kernels — `src/resampling_rcpp.cpp` (findings #5,6,7,17,18,19,25,11)

**#5 + #17 entropy at q=0 + no renormalization (`entropy_cpp` L26-129)**
- Remove the `> 1e-10` filter (L48-49); keep per-term `> 1e-15` guards (already used at L82).
- q≈0 branch (L64-76): `entropy = (double)n - 1.0;` with normalized max = `(double)n - 1.0;` — matches R `.entropy_core` (S_0 = richness − 1) and the existing test at `test-core-functions-entropy.R:192-202`.
- Remove the inner shadowing `int n` at L100 (reuse outer `n`).

**#5 (`jis_tsallis_entropy_cpp` L518-525) — align q=0 with entropy_cpp**
- q≈0: `h = (double)n_nonzero - 1.0;`; normalized max uses `(n_tx_use - 1.0)`.

**#6 + #25 divergence (`tsallis_divergence_cpp` L884-964)**
- `n` = `min` → add `Rf_warning()` (or return `NA_REAL` if lengths differ). Audit #25 says don't silently truncate; safest is warn + truncate (keep current behavior observable).
- Remove `divergence = std::abs(divergence)` (L956); use correct sign: `divergence = (1.0 - sum_pq_r)/(q - 1.0)`.
- KL branch (L906-915): when `p_i > 1e-15 && r_i <= 1e-15` → return `R_PosInf` (support violation → +∞).
- q>1 branch: when supports disjoint (`!has_valid_term`) → return `R_PosInf`, not `0.0`. For `p_i>0 && r_i==0` with q>1, term → ∞ → return `R_PosInf`.

**#7 + JIS p-value (`jis_bootstrap_delta_cpp` L846-857)**
- Center the bootstrap deltas by subtracting their mean, then `p = 2 * min(mean(centered <= 0), mean(centered >= 0))`. Remove the `method` dead param or wire it (leave accepted for ABI compat, document as percentile-only). Update the R fallback in `.compute_delta_statistics` (jackknife_isoform_switching.R L762-778) to match.

**#18 n_tx_fixed (`jis_jackknife_influences_cpp` L587,618,649)**
- Pass the original `n_tx` to both full and leave-one-out entropy calls (the R caller already passes it, but fix the default trap).

**#19 matrix orientation (jackknife vs jis entropy)**
- Exploration confirmed both `jackknife_resampling_cpp` and `jis_tsallis_entropy_cpp` compute entropy over columns consistently. No code change; add a clarifying comment + assertion that rows=transcripts, cols=samples.

**#11 new `resample_by` mode (bootstrap_compute_cpp)**
- Add a new exported `bootstrap_replicate_cpp(x, replicate_indices, nboot, ...)` that resamples replicate *indices* with replacement (paired/block-aware), then recomputes the statistic per resampled replicate set. Add the R-side argument `resample_by` defaulting to `"read"` in `.bootstrap_resample_optimized` / `.bootstrap_compute_ci` and dispatch accordingly.

---

## B. Bootstrap CI — R layer (findings #3,4,12,15,20,21,22,16)

**#3 + #4 BCa acceleration (`diversity_helpers.R:.ci_bca` L825-890, `bootstrap.R:.bca_ci` L1775-1865)**
- Replace fake leave-one-out-of-bootstrap with **true jackknife** using `.jackknife_compute_estimates` (`R/jackknife_diagnostics.R:566`). Add a `.bca_acceleration(x, stat_fn, ...)` helper shared by both `.ci_bca` and `.bca_ci`.
- `.bootstrap_compute_ci` (bootstrap.R L860): pass the already-normalized `x_for_calc` and the already-computed `point_est` into `.ci_bca` instead of re-deriving from raw `x`.

**#12 z0 padding & strict comparison (both functions)**
- `prop_less = (sum(bootstrap_dist < point_est) + 0.5) / B`; delete the `<=`/non-strict variants and the silent `z0<-0` reset.

**#15 (#21) jackknife SE double-/n (`bootstrap.R` L2377-2379)**
- `jack_se <- sqrt(jack_var)` (remove the extra `/n`).

**#20 RNG reproducibility (`parallelization.R` L34-48, `bootstrap_divergence.R` L164)**
- Add `seed` to `.get_bpparam` and pass `RNGseed = seed` to `MulticoreParam`/`SnowParam`. Add `seed` to `.bootstrap_divergence` signature (it's already documented at L50) and thread it through.

**#22 JIS paired delta ignores pairing (`jackknife_isoform_switching.R` L739-751 + C++ L740-741)**
- When `paired`, resample a single index vector over pairs and select both A and B columns together (R fallback + C++ path).

**#16 LMM Strategy 4 drops pairing (`sait_lmm.R` L217-227)**
- Add an explicit `warning()` (not verbose-only) when pairing is dropped; propagate a `pairing_dropped = TRUE` flag into the result frame so it's visible to users.

---

## C. SAIT statistical testing (findings #1,2,8,9,10,13,14,15,23,24)

**#1 + #2 GAM LRT validity (`sait_gam.R`)**
- Pass `method = "ML"` to every `mgcv::gamm()` call (`.fit_gamm_ar1_single` L500-504, `.fit_gamm_nocorr_single` L545-549, and any other `gamm()`).
- Alt model: `entropy ~ group + s(q, bs="tp", k=K) + s(q, bs="tp", k=K, by=group)` with the **same K** in null and alt (`K` from `k_q_marginal`). Update `.fit_gamm_ar1` (L526-531), `.fit_gamm_fallback` (L562-566), `.fit_standard_gam` (L629-640).
- Use `mgcv::compareML()` in `.compare_gam_models` (L587) for GAMM, falling back to `anova(..., test="Chisq")`.

**#8 remove `.gam_bias_correct` (`sait_gam.R` L1038-1174, call site L192)**
- Delete the function and its invocation; remove `bias_correction` handling from the GAM path. Remove the ~10 tests in `test-statistical-methods-sait_gam.R` and `test-statistical-methods-sait_helpers.R` that assert p-value inflation.

**#9 FPCA gene-level test (`sait_fpca.R` L467-477)**
- Replace `min(BH-adjusted PC p-values)` with **Hotelling's T²** (2 groups) / **MANOVA** (>2 groups) on the selected PC scores. Add a `.fpca_hotelling_test` helper.

**#10 LMM default AR(1) (`sait_helpers_fit.R` L336-360)**
- Add `correlation = nlme::corAR1(form = ~1 | subject)` to the primary `nlme::lme()` calls. Remove the dead `use_var_structure` branch.

**#13 QIC (`sait_gee.R` L902-911)** — implement Pan (2001): `QIC = -2*quasi_ll + 2*trace(R_i^{-1} V_R)` (or the standard `2*trace` model-complexity penalty), replacing `1*log(n_obs)`/`0`.

**#14 Kauermann-Carroll (`sait_gee.R` L731-766)** — actually apply `vcov_corrected` (recompute z-stat with the HC1-scaled covariance) instead of discarding it; rename if behavior stays a t-dist swap.

**#15 GEE joint interaction (`sait_gee.R` L370-414, L437-473)** — joint Wald test `β' V⁻¹ β ~ χ²(df)` across all `q:` coefficients (handles multi-level groups).

**#23 Westfall-Young call site (`sait_core.R` L355-357)** — pass `fit_one_fn`, `rownames_mat`, `mat`, `se`, `method`, `pvalue`, `subject_col`, `paired`, `min_obs`, `nthreads`, `bias_correction`, `regularization`, `corstr`, `adaptive_knots` into `.adjust_pvalues_multicorr` so the W-Y refit can compute per-gene permutation p-values.

**#24 duplicate `.ar1_design_effect_memo` (`sait_gam.R` L239-271 vs `sait_helpers.R` L90-122)** — delete the GAM-local asymptotic `(1+rho)/(1-rho)` version; keep the finite-m Diggle form in `sait_helpers.R` (used by `.clear_sait_helper_cache`).

---

## D. Tests & verification
- Update affected expectations (entropy q=0 now `n-1`; GAM LRT now ML+main-effect; FPCA Hotelling; GEE joint Wald; W-Y; JIS centered p-values). Remove the ~10 `.gam_bias_correct` tests.
- Add focused unit tests for the new/corrected paths: true-jackknife BCa acceleration, divergence `+Inf` on support violation, JIS centered bootstrap p-value, Hotelling T², joint Wald, `resample_by="replicate"`.
- After C++ edits: run `Rcpp::compileAttributes()` (via `Rscript -e 'Rcpp::compileAttributes()'`), then `R CMD build` + `R CMD check` (or `devtools::test()`) to confirm the suite passes.

## Sequencing
1. C++ kernels (A) → compileAttributes → build → run rcpp tests.
2. Bootstrap CI R layer (B) → run bootstrap/jis tests.
3. SAIT testing (C) → run sait tests; remove `.gam_bias_correct` tests.
4. Update/add tests (D), full `R CMD check`.