% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jackknife_diagnostics.R
\name{jackknife_isoform_switching}
\alias{jackknife_isoform_switching}
\title{Jackknife Isoform Switching Detection}
\usage{
jackknife_isoform_switching(
  se = NULL,
  condition_col = "condition",
  pair_col = NULL,
  gene_col = NULL,
  isoform_col = NULL,
  top_n = 5,
  q = 1,
  norm = TRUE,
  log_base = exp(1),
  pseudocount = 0,
  threshold = 90,
  print_results = TRUE,
  verbose = FALSE,
  lm_results = NULL,
  lm_p_threshold = 0.05,
  use_lm_fdr = TRUE
)
}
\arguments{
\item{se}{A SummarizedExperiment object with transcript-level counts.}

\item{condition_col}{Character: column name in colData for condition labels.}

\item{pair_col}{Character: optional column name for paired design (individual IDs).}

\item{gene_col}{Character: column name in rowData for gene IDs.}

\item{isoform_col}{Character: column name in rowData for transcript/isoform IDs.}

\item{top_n}{Numeric: number of top genes to analyze (default 5). If NULL, all genes.}

\item{q}{Numeric: Tsallis entropy order (default 1 = Shannon entropy).}

\item{norm}{Logical: normalize entropy to [0,1]? (default TRUE).}

\item{log_base}{Numeric: log base for entropy (default e).}

\item{pseudocount}{Numeric: pseudocount to add (default 0).}

\item{threshold}{Numeric: percentile for outlier detection on influences (default 90).}

\item{print_results}{Logical: print results? (default TRUE).}

\item{verbose}{Logical: verbose output? (default FALSE).}

\item{lm_results}{Data frame: results from calculate_lm_interaction() with 'gene',
'p_interaction' and/or 'adj_p_interaction' columns for filtering genes.}

\item{lm_p_threshold}{Numeric: p-value threshold for LM gene filtering (default 0.05).}

\item{use_lm_fdr}{Logical: use adjusted p-values from LM results if available (default TRUE).}
}
\value{
A list of class tsenat_isoform_switching with:
 - results_per_gene: named list of per-gene results
 - summary_table: data.frame with per-gene summary
 - all_transcript_stats: data.frame with all transcript statistics
 - gene_names: character vector of analyzed genes
 - conditions: character vector of the two conditions compared
 - metadata: list with analysis metadata
}
\description{
Detects isoform/transcript switches between conditions using condition-stratified
jackknife analysis on Tsallis entropy. Identifies which transcripts change
importance between conditions (e.g., condition A vs B).
}
