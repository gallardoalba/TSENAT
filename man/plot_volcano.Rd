% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_plots.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot for differential results}
\usage{
plot_volcano(
  diff_df,
  x_col = "mean_difference",
  padj_col = "adjusted_p_values",
  label_thresh = 0.1,
  padj_thresh = 0.05,
  top_n = 5
)
}
\arguments{
\item{diff_df}{Data.frame from `calculate_difference()` containing at least
`mean_difference` and an adjusted p-value column (default
`adjusted_p_values`).}

\item{x_col}{Column name for x-axis (default `mean_difference`).}

\item{padj_col}{Column name for adjusted p-values (default
`adjusted_p_values`).}

\item{label_thresh}{Absolute x threshold to mark significance (default 0.1).}

\item{padj_thresh}{Adjusted p-value cutoff (default 0.05).}

\item{top_n}{Integer; number of top genes to annotate by smallest adjusted
p-value (default: 5).}
}
\value{
ggplot volcano plot.
}
\description{
Volcano plot for differential results
}
\examples{
df <- data.frame(
gene = paste0("g", seq_len(10)),
mean_difference = runif(10),
adjusted_p_values = runif(10)
)
plot_volcano(df, x_col = "mean_difference", padj_col = "adjusted_p_values")
}
