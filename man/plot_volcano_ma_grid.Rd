% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_plots.R
\name{plot_volcano_ma_grid}
\alias{plot_volcano_ma_grid}
\title{Combine Volcano and MA-Tsallis Plots in a Grid Layout}
\usage{
plot_volcano_ma_grid(
  diff_df,
  x_col = NULL,
  padj_col = "padj",
  label_thresh = 0.1,
  sig_alpha = 0.05,
  top_n = 5,
  title_volcano = NULL,
  title_ma = "Tsallis-based MA plot",
  ...
)
}
\arguments{
\item{diff_df}{Data.frame from differential analysis containing required columns for both volcano and MA plots.}

\item{x_col}{Column name for x-axis in volcano plot (e.g., "mean_difference"). Auto-detected if NULL.}

\item{padj_col}{Column name for adjusted p-values (default: "padj").}

\item{label_thresh}{Threshold for volcano plot labels (default: 0.1).}

\item{sig_alpha}{Numeric significance threshold for adjusted p-values (default: 0.05).}

\item{top_n}{Number of top genes to annotate in volcano plot (default: 5).}

\item{title_volcano}{Title for volcano plot. If NULL, auto-generated.}

\item{title_ma}{Title for MA plot (default: "Tsallis-based MA plot").}

\item{...}{Additional arguments passed to plotting functions.}
}
\value{
A `ggplot2` object showing a 1×2 grid with volcano plot on the left and MA plot on the right.
}
\description{
Creates a side-by-side grid layout with a volcano plot on the left and an MA-Tsallis plot on the right.
Both plots are generated from differential analysis results data.
}
\examples{
# Simulate differential analysis results
x <- data.frame(
  genes = paste0("g", seq_len(20)),
  mean_difference = rnorm(20, sd = 1),
  padj = runif(20, 1e-5, 0.1),
  log2_fold_change = rnorm(20, sd = 0.8)
)
# Placeholder: actual usage would require valid differential results
# plot_volcano_ma_grid(x, sig_alpha = 0.05)

}
