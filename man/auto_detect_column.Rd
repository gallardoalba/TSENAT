% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_utils.R
\name{auto_detect_column}
\alias{auto_detect_column}
\title{Auto-Detect Column from Candidate Names}
\usage{
auto_detect_column(
  available_cols,
  config_list = NULL,
  config_key = NULL,
  priority_candidates = character(0),
  default_fallback = NULL,
  verbose = FALSE,
  param_name = NULL
)
}
\arguments{
\item{available_cols}{Character vector of available column names}

\item{config_list}{Configuration list with optional priority column name}

\item{config_key}{Key name in config_list to check first}

\item{priority_candidates}{Character vector of candidate column names to try in order}

\item{default_fallback}{Character string. Fallback if no matches found. Default: NULL (no fallback).}

\item{verbose}{Logical. Print auto-detection messages. Default: FALSE.}

\item{param_name}{Parameter name for messages (e.g., "condition_col"). Required for verbose.}
}
\value{
Character string with detected column name, or NULL if not found and no fallback
}
\description{
Search for a column by trying multiple candidate names with fallback options.
}
\details{
Priority resolution order:
1. If \code{config_key} exists in \code{config_list}, check if it's in available columns
2. Check each \code{priority_candidates} in order against available columns
3. Return \code{default_fallback} if provided
4. Return NULL otherwise

This function centralizes the complex auto-detection logic used by multiple
S4 wrapper functions for finding condition_col, gene_col, isoform_col, etc.
}
\examples{
available <- c("sample_type", "group", "condition", "other")

# Config takes priority
col <- auto_detect_column(available, list(col = "sample_type"),
                          config_key = "col", 
                          priority_candidates = c("condition", "group"))
# Returns: "sample_type"

# Falls back to priority candidates
col <- auto_detect_column(available, list(other = "value"),
                          config_key = "col", 
                          priority_candidates = c("condition", "group"))
# Returns: "group" (first match)

}
\keyword{internal}
