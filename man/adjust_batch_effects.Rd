% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_effect_handling.R
\name{adjust_batch_effects}
\alias{adjust_batch_effects}
\title{Adjust Batch Effects in RNA-seq Count Data (Unified Interface)}
\usage{
adjust_batch_effects(
  se,
  batch,
  method = c("standard", "ref"),
  ref_batch = NULL,
  group = NULL,
  shrinkage = TRUE,
  par.prior = TRUE,
  mean.only = FALSE
)
}
\arguments{
\item{se}{A SummarizedExperiment object with count matrix (genes * samples)}

\item{batch}{Character; name of batch column in colData(se)}

\item{method}{Character; one of "standard" (default) or "ref"
- "standard": Apply ComBat-seq to all batches (standard batch correction)
- "ref": Preserve reference batch, adjust others relative to it (ComBat-ref)}

\item{ref_batch}{Character; identifier of reference batch (required if method="ref")}

\item{group}{Character; optional biological group variable to preserve}

\item{shrinkage}{Logical; use empirical Bayes shrinkage (default: TRUE, for "standard" only)}

\item{par.prior}{Logical; parametric prior (default: TRUE, for "standard" only)}

\item{mean.only}{Logical; adjust only mean, not dispersion (default: FALSE)}
}
\value{
SummarizedExperiment with batch-corrected counts in assay slot
}
\description{
Removes batch effects from count data using ComBat-seq algorithm (Zhang et al., 2020).
Provides unified interface supporting standard correction and reference-batch preservation.
}
\examples{
\dontrun{
# Standard batch correction (adjust all batches)
corrected_standard <- adjust_batch_effects(
  se = readcounts_se,
  batch = "batch_id",
  method = "standard"
)

# Reference-batch correction (preserve primary batch)
corrected_ref <- adjust_batch_effects(
  se = readcounts_se,
  batch = "batch_id",
  method = "ref",
  ref_batch = "primary_collection"
)
}
}
\references{
Zhang, Y., et al. (2020). ComBat-seq: batch effect adjustment for 
RNA-seq count data. NAR Genomics and Bioinformatics, 2(3), lqaa078.

Hornung, R., et al. (2024). ComBat-ref: Batch effect correction with 
reference batch preservation. bioRxiv preprint.
}
