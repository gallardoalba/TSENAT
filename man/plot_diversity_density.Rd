% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_plots.R
\name{plot_diversity_density}
\alias{plot_diversity_density}
\title{Plot diversity distributions (density) by sample type}
\usage{
plot_diversity_density(se, assay_name = "diversity", sample_type_col = NULL)
}
\arguments{
\item{se}{A `SummarizedExperiment` returned by `calculate_diversity`.}

\item{assay_name}{Name of the assay to use (default: "diversity").}

\item{sample_type_col}{Optional column name in `colData(se)` with sample types.
If missing, sample types are inferred from column names (suffix after
the last underscore) or set to 'Group'.}
}
\value{
A `ggplot` object with layered density plots.
}
\description{
Plot diversity distributions (density) by sample type
}
\examples{
data("tcga_brca_luma_dataset", package = "TSENAT")
rc <- as.matrix(tcga_brca_luma_dataset[1:20, -1, drop = FALSE])
gs <- tcga_brca_luma_dataset$genes[1:20]
se <- calculate_diversity(rc, gs, q = 0.1, norm = TRUE)
plot_diversity_density(se)
}
