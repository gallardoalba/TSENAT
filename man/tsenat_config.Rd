% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestration_tsenat.R
\name{tsenat_config}
\alias{tsenat_config}
\title{Create and return TSENAT configuration}
\usage{
tsenat_config(
  q_values = NULL,
  q_range = NULL,
  filter_genome = TRUE,
  formula = NULL,
  p_threshold = 0.05,
  fdr_threshold = 0.05,
  methods = NULL,
  generate_plots = TRUE,
  plot_types = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{q_values}{\code{numeric}. Q-values for Tsallis entropy spectrum.
Default: \code{seq(0.5, 2.0, by = 0.5)}.}

\item{q_range}{\code{numeric}. Alternative to q_values: lower and upper bounds.}

\item{filter_genome}{\code{logical}. Remove zero rows before analysis.
Default: TRUE.}

\item{formula}{\code{formula}. Model formula for LM interactions (e.g., \code{~ treatment}).}

\item{p_threshold}{\code{numeric}. P-value threshold for significance.
Default: 0.05.}

\item{fdr_threshold}{\code{numeric}. FDR threshold (Benjamini-Hochberg).
Default: 0.05.}

\item{methods}{\code{character}. Analysis methods to run. Options:
"diversity", "lm_interaction", "jackknife", "divergence",
"q_interactions", "difference". Default: all methods.}

\item{generate_plots}{\code{logical}. Generate visualizations.
Default: TRUE.}

\item{plot_types}{\code{character}. Specific plots to generate.
Default: all available types.}

\item{seed}{\code{numeric}. Random seed for reproducibility.}

\item{...}{Additional configuration parameters (stored as-is).}
}
\value{
\code{list} with class \code{TSENATConfig} containing all
  specified parameters.
}
\description{
Builds a configuration list for use with \code{\link{tsenat}}().
Allows specifying analysis parameters once and reusing across multiple
analyses.
}
\details{
Configuration is stored in the TSENATAnalysis@config slot and used
by wrapper functions to configure analysis behavior.
}
\examples{
# Default config with standard parameters
cfg <- tsenat_config()

# Custom spectrum and formula
cfg <- tsenat_config(
  q_values = c(0.5, 1.0, 1.5, 2.0),
  formula = ~ treatment + batch,
  fdr_threshold = 0.01
)

# Use with tsenat()
\dontrun{
  analysis <- tsenat(se, config = cfg)
}

}
