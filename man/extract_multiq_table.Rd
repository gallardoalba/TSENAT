% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s4_functions_utils.R
\name{extract_multiq_table}
\alias{extract_multiq_table}
\title{Extract Multi-Q Tabular Results with Q-Value Column}
\usage{
extract_multiq_table(
  result,
  is_multiq = NULL,
  extract_fn = NULL,
  q_value_col = "q_value"
)
}
\arguments{
\item{result}{Result object (may be list with multi-q structure or single
result)}

\item{is_multiq}{Logical. Whether result has multi-q structure. Default:
auto-detect.}

\item{extract_fn}{Function to extract table from each result element.
 Signature:  \code{function(result_element,  q_key)}.  Default:
extracts 'summary_table'.}

\item{q_value_col}{Name of q-value column to add. Default: 'q_value'}
}
\value{
data.frame with combined results and q_value column
}
\description{
Helper to combine results from multiple q-values into a single data.frame
with q_value column.
}
\examples{
# Create sample multi-q result structure
q_result <- list(
  q_0.5 = list(summary_table = data.frame(
    gene_id = c('GENE1', 'GENE2'),
    entropy = c(1.2, 1.5),
    psi_mean = c(0.3, 0.7)
  )),
  q_1.0 = list(summary_table = data.frame(
    gene_id = c('GENE1', 'GENE2'),
    entropy = c(1.1, 1.4),
    psi_mean = c(0.32, 0.68)
  ))
)

# Extract and combine results across q-values
combined_results <- extract_multiq_table(q_result)
head(combined_results)

}
