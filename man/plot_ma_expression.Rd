% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_plots.R
\name{plot_ma_expression}
\alias{plot_ma_expression}
\title{Plot MA using expression/readcount-based fold changes}
\usage{
plot_ma_expression(
  x,
  se,
  samples = NULL,
  control = NULL,
  fc_method = "median",
  pseudocount = 0,
  sig_alpha = 0.05,
  x_label = NULL,
  y_label = NULL,
  title = NULL,
  ...
)
}
\arguments{
\item{x}{Data.frame from `calculate_difference()`.}

\item{se}{A `SummarizedExperiment` or data.frame supplying readcounts or precomputed fold changes.}

\item{samples}{Optional sample grouping vector (passed to `plot_ma`).}

\item{control}{Control level name (passed to `plot_ma`).}

\item{fc_method}{Aggregation method for fold-change calculation (passed to `plot_ma`).}

\item{pseudocount}{Pseudocount added when computing log ratios (passed to `plot_ma`).}

\item{sig_alpha}{Numeric significance threshold for adjusted p-values (default: 0.05).}

\item{x_label}{Optional x-axis label passed to `plot_ma`.}

\item{y_label}{Optional y-axis label passed to `plot_ma`.}

\item{title}{Optional plot title passed to `plot_ma`.}

\item{...}{Additional arguments passed to `plot_ma()`.}
}
\value{
A `ggplot2` object representing the MA plot.
}
\description{
Wrapper around `plot_ma(..., type = "expression")` that accepts a
`SummarizedExperiment` or precomputed fold-change `data.frame`.
}
\examples{
x <- data.frame(genes = paste0("g", seq_len(5)), mean = runif(5))
fc <- data.frame(genes = paste0("g", seq_len(5)), log2_fold_change = rnorm(5))
plot_ma_expression(x, se = fc)
}
