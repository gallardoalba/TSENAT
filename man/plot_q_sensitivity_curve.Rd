% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jackknife_diagnostics.R
\name{plot_q_sensitivity_curve}
\alias{plot_q_sensitivity_curve}
\title{Q-Parameter Sensitivity Curve}
\usage{
plot_q_sensitivity_curve(
  se = NULL,
  condition_col = "condition",
  gene = NULL,
  gene_col = NULL,
  isoform_col = NULL,
  q_values = c(0.5, 1, 1.5, 2, 2.5),
  norm = TRUE,
  log_base = exp(1),
  pseudocount = 0,
  main = NULL,
  show_legend = TRUE
)
}
\arguments{
\item{se}{A SummarizedExperiment object with transcript-level counts.}

\item{condition_col}{Character: column name in colData for condition labels.}

\item{gene}{Character: name of the gene to plot.}

\item{gene_col}{Character: column name in rowData for gene IDs.}

\item{isoform_col}{Character: column name in rowData for transcript/isoform IDs.}

\item{q_values}{Numeric: vector of q values to test (default c(0.5, 1, 1.5, 2, 2.5)).}

\item{norm}{Logical: normalize entropy to [0,1]? (default TRUE).}

\item{log_base}{Numeric: log base for entropy (default e).}

\item{pseudocount}{Numeric: pseudocount to add (default 0).}

\item{main}{Character: plot title.}

\item{show_legend}{Logical: show legend with transcript names? (default TRUE).}
}
\value{
Invisibly returns a data.frame with delta_influence for each q and transcript.
}
\description{
Plots how isoform switching effects change across different q-values,
revealing scale-dependent diversity patterns (paper I004).
}
\details{
Q-parameter interpretation:
- \strong{q < 1}: Emphasizes rare transcripts
- \strong{q = 1}: Shannon entropy (balanced weighting)
- \strong{q > 1}: Emphasizes abundant transcripts
- \strong{q spectrum}: Different genes may show optimal switching at different q

References: \strong{S111} (q-parameter sensitivity), \strong{I004} (q-parameter theory)
}
\examples{
\dontrun{
# Plot switching effects across q values for a specific gene
plot_q_sensitivity_curve(
  se = ts_se,
  condition_col = "condition",
  gene = "Gene1",
  gene_col = "gene_id",
  isoform_col = "isoform_id"
)
}

}
