% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_diversity.R
\name{compute_posterior_credible_intervals}
\alias{compute_posterior_credible_intervals}
\title{Compute Posterior Credible Intervals for Multiple Genes}
\usage{
compute_posterior_credible_intervals(counts_matrix, alpha, beta, ci = 0.95)
}
\arguments{
\item{counts_matrix}{Matrix or data.frame; genes (rows) * samples (columns).}

\item{alpha}{Numeric; alpha parameter of Beta prior.}

\item{beta}{Numeric; beta parameter of Beta prior.}

\item{ci}{Numeric; credible interval width (default: 0.95).}
}
\value{
Data frame with columns:
  \describe{
    \item{gene}{Gene name (from rownames of counts_matrix)}
    \item{posterior_mean}{Point estimate}
    \item{ci_lower}{Lower CI bound}
    \item{ci_upper}{Upper CI bound}
    \item{posterior_sd}{Standard deviation}
  }
}
\description{
Extracts posterior credible intervals for all genes in a count matrix,
useful for visualizing uncertainty across the genome.
}
\examples{
# Fit prior
prior_params <- fit_empirical_beta_prior(counts)

# Compute CIs for all genes
all_cis <- compute_posterior_credible_intervals(
  counts_matrix = counts,
  alpha = prior_params$alpha,
  beta = prior_params$beta,
  ci = 0.95
)

# View top genes by posterior mean
head(all_cis[order(-all_cis$posterior_mean), ], 10)

}
