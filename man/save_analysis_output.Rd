% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_utils.R
\name{save_analysis_output}
\alias{save_analysis_output}
\title{Centralized Output File Saving for Analysis Results}
\usage{
save_analysis_output(
  data,
  output_file,
  object = NULL,
  verbose = FALSE,
  create_dir = TRUE,
  func_name = "wrapper_function",
  width = NULL,
  height = NULL
)
}
\arguments{
\item{data}{Object to save (data.frame, matrix, ggplot, SummarizedExperiment, or TSENATAnalysis)}

\item{output_file}{File path. Format auto-detected from extension (.rds, .tsv, .csv, .txt, .pdf, .png, .jpg)}

\item{object}{Optional parent object (e.g., TSENATAnalysis) for context-aware saving. Default: NULL.}

\item{verbose}{Logical. Print saving status. Default: FALSE.}

\item{create_dir}{Logical. Create directory if it doesn't exist. Default: TRUE.}

\item{func_name}{Function name for error messages (e.g., "calculate_diversity_s4"). Default: "wrapper_function"}
}
\value{
Invisibly returns TRUE if successful, FALSE if error suppressed
}
\description{
Save analysis results to file with automatic format detection and handling.
}
\details{
**Supported formats:**
\itemize{
  \item \code{.rds}: R serialized object (for S4 objects, TSENATAnalysis)
  \item \code{.tsv, .csv, .txt}: Tables (for data.frame, matrix, extracted tables)
  \item \code{.pdf}: PDF (for ggplot objects)
  \item \code{.png, .jpg}: Raster image (for ggplot objects)
}

**Auto-detection logic:**
\itemize{
  \item ggplot object → saves as .pdf or .png/.jpg if extension matches
  \item data.frame → saves as .tsv/.csv/.txt
  \item SummarizedExperiment/matrix → converts to data.frame, saves as table
  \item TSENATAnalysis → saves as .rds (S4 object preservation)
  \item Other → attempts default save based on extension
}

Errors in saving only warn (not stop) to prevent entire analysis failure if output
file cannot be written.
}
\examples{
# Save a data frame
df <- data.frame(gene = c("GENE1", "GENE2"), value = c(1.5, 2.3))
save_analysis_output(df, "results/output.tsv")

# Save a ggplot object
p <- ggplot(data.frame(x = 1:10, y = rnorm(10)), aes(x, y)) + geom_point()
save_analysis_output(p, "results/plot.pdf")

}
