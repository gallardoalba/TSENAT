% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_factories.R
\name{create_test_analysis}
\alias{create_test_analysis}
\title{Create Test Analysis Data}
\usage{
create_test_analysis(
  n_genes = 8,
  n_samples_per_group = 20,
  control_lambda = 40,
  treatment_lambda = 150,
  q_values = c(0.5, 1, 1.5),
  seed = 42,
  verbose = FALSE
)
}
\arguments{
\item{n_genes}{Number of genes to simulate (default: 8)}

\item{n_samples_per_group}{Samples per condition (default: 20)}

\item{control_lambda}{Poisson lambda for control (default: 40)}

\item{treatment_lambda}{Poisson lambda for treatment (default: 150)}

\item{q_values}{Q-value vector (default: c(0.5, 1.0, 1.5))}

\item{seed}{Random seed (default: 42)}

\item{verbose}{Progress messages (default: FALSE)}
}
\value{
TSENATAnalysis with diversity results across q-values
}
\description{
Factory functions that generate consistent, reusable test data 
with biological signal for examples and tests.
}
\details{
Eliminates repetitive setup code (~70% reduction). Ensures:
- Biological signal (control lambda=40 vs treatment lambda=150)
- Sufficient samples (40 total: 20 per group)
- Multiple q-values avoid rank deficiency
}
\examples{
# Create with defaults
analysis <- create_test_analysis()
head(diversity(analysis, q = 1.0))

}
